# Loaded when ACTIVE_MODEL=bge-m3 (in config/server.env). Same name as this file.
# BGE-M3 embedding (BAAI), FP16 ~1.2 GB. For vector/semantic search.
gguf: bge-m3-FP16.gguf
download_url: "https://huggingface.co/gpustack/bge-m3-GGUF"

context_size: 0
n_gpu_layers: -1
jinja: true
temp: 0
top_p: 1.0
top_k: 40
min_p: 0.01
seed: 3407
batch_size: 512
ubatch_size: 256

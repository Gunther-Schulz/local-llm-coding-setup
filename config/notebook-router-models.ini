# Per-model presets for notebook router (--models-dir). Section name = model name (no .gguf).
# bge-m3 child must be started with --embeddings so /v1/embeddings works when model=bge-m3.
[bge-m3]
embeddings = true

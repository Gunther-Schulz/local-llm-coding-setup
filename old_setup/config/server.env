# Active model key (must match a subdir under models/ and a file config/models/<key>.env)
ACTIVE_MODEL=qwen3-coder-next-mxfp4

# Server (override port with: ./run_server.sh PORT)
PORT=8000
HOST=127.0.0.1

# RunPod LLM Server Requirements
# Install llama-cpp-python separately with CUDA support:
# pip install --upgrade llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121 --no-cache-dir

# Core dependencies with minimum versions (aligned with vLLM common.txt)
jinja2>=3.1.0
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.27.0
pydantic>=2.12.0
requests>=2.31.0
httpx>=0.27.0

# LLM and compression
llmlingua>=0.2.1
sentence-transformers>=2.3.0
tiktoken>=0.6.0  # For accurate token counting in compression proxy
# vllm: for RTX 5090 use ./install-vllm-5090.sh (builds from source; prebuilt lacks sm_100). Omit when using that script.
vllm

# Hugging Face (hf download fallback; hf_transfer helps when aria2/curl not used)
huggingface_hub[cli]>=0.32.0
hf_transfer>=0.1.5

# Additional server dependencies
sse-starlette>=2.0.0
anyio>=4.0.0


